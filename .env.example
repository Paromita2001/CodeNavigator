# Optional. Enables the "AI Answer" synthesis step (natural-language answers
# grounded in retrieved code snippets). Get a free key at console.groq.com.
# Without this, the app still works — it just shows raw ranked snippets.
GROQ_API_KEY=
